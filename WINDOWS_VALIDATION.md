# Windows 验证包使用说明

构建日期：2026-09-20。架构：Windows x64。包含本地 Core、WPS 表格／文字／演示加载项、统一侧栏、变量版本和原稿修改历史。无需 Node.js、Go 或 wpsjs，无需管理员权限。本包未进行代码签名，真实 Windows WPS 操作由用户验收。

## 当前版本：0.8.0-js1

AI 新生成的计算和展示规则改用 JavaScript；优先按照 JAVASCRIPT_VALIDATION.md 重测本次问题。

## 保留 hotfix1 安装修复

修复升级安装时 Core.exe 被旧进程占用导致失败：按完整路径枚举本插件进程，不再依赖健康检查、版本号、端口或 PowerShell；确认退出后再替换文件。替换使用暂存文件及有限重试，失败时保留原文件。无需先卸载，项目数据保留。

## 安装和网络

1. 将整个 ZIP 复制到 Windows 设备的本地目录，完整解压。
2. 保存文件并完全退出 WPS。
3. 双击 `DataReportAssistant-Setup-0.8.0-js1.exe`，按提示安装。
4. 重开 WPS，在“数据报告助手”选项卡点击“打开助手”。
5. 如需 AI，在侧栏设置填写模型服务的 Base URL、模型名称及 API Key。

每台 Windows 设备运行自己的本地服务，监听 `127.0.0.1:17891`，不需要连接 Mac 的插件服务或对局域网开放此端口。若 AI 模型运行在另一台局域网设备，Base URL 应填写那台设备的可访问 IP 和模型端口，例如 `http://192.168.1.10:8000/v1`（仅为示例，替换为实际地址），不能填本机回环地址。

安装位置：`%LOCALAPPDATA%\DataReportAssistant\app`；项目数据：`%LOCALAPPDATA%\DataReportAssistant\data`；插件注册：`%APPDATA%\kingsoft\wps\jsaddons\publish.xml`。Core 随当前用户登录启动；不会覆盖其他插件注册。卸载可在 Windows 已安装应用中选择“数据报告助手”，项目数据会保留。

## 建议验收顺序

使用包内 samples 的测试表格与演示；另建一份含占位文本和 2×2 表格的文字文稿并保存。不要直接覆盖唯一的业务原稿。

| 检查 | 操作与预期 |
|---|---|
| 加载 | 表格、文字、演示均能打开统一侧栏，显示正确的当前文件 |
| 变量创建 | 创建项目，读取表格选区，生成“金额合计”，检查结果后保存 |
| 变量修正 | 检查与修改，改成“金额平均值”，对比当前版本与新结果；返回修改不应保存 |
| 变量恢复 | 保存修改后，从变量历史恢复旧版本，值与规则恢复，输出不自动改变 |
| 通用输入 | 将演示和文字文件加入同一项目，从选中的文字创建变量 |
| 通用输出 | 选择项目变量，分别绑定表格空单元格、演示文本框、文字占位文本；生成方案不改原稿，点击应用后原稿改变 |
| 历史撤销 | 查看修改前后内容，撤销最近修改，检查文字、单元格及格式恢复 |
| 重载 | 应用后刷新侧栏或保存关闭再打开文件，历史仍存在且可撤销 |
| 冲突保护 | 应用后手工改动目标，再撤销；应提示冲突并保留人工修改 |
| 表格输出 | 向空间足够的现有表格写入表格变量并撤销；空间不足、合并区域应拒绝写入 |
| 输入格式 | 用 ods / odt / odp 等 WPS 可打开文件测试，输入输出方向由宿主能力而非后缀决定 |

当前不是所有 WPS 对象都已实现：内置单元格、文字选区／表格和幻灯片文本／表格；图片、图表、PDF 等需扩展能力。文字格式恢复依赖 WPS 提供 WordOpenXML、InsertXML 和书签接口，缺少接口时会提示并阻止写入。详细边界见 HOST_CAPABILITIES.md。

## 排查与反馈

- 侧栏提示服务未启动：访问 `http://127.0.0.1:17891/`，检查 `%LOCALAPPDATA%\DataReportAssistant\data\core.log`。服务正常不代表插件加载已成功。
- WPS 中无插件入口：确认已完全退出并重开 WPS，记录 WPS 版本与 publish.xml 是否存在；不要为了测试关闭系统安全保护。
- 反馈请包含：Windows 版本、WPS 版本、表格／文字／演示、操作步骤、错误文本与截图。
- 可在侧栏设置导出诊断包，默认不含业务数据。不要提供 API Key。

此构建已通过 Core 测试／vet、安装器辅助测试、17 项 JS 模拟测试，并成功交叉编译为 Windows PE x64。安装／宿主 API 的真实 Windows 行为尚待验收。
