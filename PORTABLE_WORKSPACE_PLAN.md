# 通用 WPS 数据工作区：实施与验收

目标保持完整：输入、输出均与文件格式解耦，以可扩展宿主能力连接任意 WPS 可处理文件；统一侧栏；支持 macOS WPS；模拟用户进行本地验证。

## 交付要求

- 通用 Source 定位信息与读取能力，不再限制 Excel；通用 Binding/修改记录，不再限制 PPT。
- 可注册宿主适配器：注册读取、目标定位、写入、快照、恢复能力。首批覆盖表格、文字、演示的双向使用，不能以文件扩展名替代能力判断。
- 统一“项目数据 / 输出 / 修改历史”侧栏，所有宿主均可创建、检查、修改变量，并绑定输出、检查实际结果、撤销。
- macOS 安装、启动服务和卸载路径，保留其他插件及业务数据；Windows 发布保持可用。
- 真实 Mac WPS 加载与 UI 操作验证；自动化模拟宿主覆盖格式/方向组合、能力扩展、错误和撤销；明确区分模拟结果与真实 WPS 结果。

## 当前基线（2026-09-19）

- 已有 ET 输入与 WPP 输出、PPT 修改历史。Core 对 Document.Kind 有硬编码限制；尚无 Writer 宿主。
- 本机 WPS 12.1.25869，原生界面可操作。安装包包含 jsapiservice 与 jswpsapiex。
- WPS 沙箱配置目录已可访问；已写入 ET/WPP/WPS 注册，安装并启动本地 Core。
- WPS 官方概述主要标注 Windows/Linux；Mac 的实际注册与加载必须以本机验证为准。

## 推进顺序

1. 通用 Core 数据契约、变量修改版本与历史兼容迁移。
2. 宿主能力注册表与 ET/WPS/WPP 双向适配。
3. 统一侧栏和本地/跨宿主操作测试。
4. macOS 安装与原生 WPS 端到端验证；用户于 2026-09-20 明确选择自行执行真实 WPS 验收，代理交付安装包和步骤。
5. 按以上每一项证据审计后才能标记目标完成。

## 本轮交付证据（2026-09-20）

- 通用宿主注册表、三个内置双向能力、统一侧栏已实现；自定义能力和 16 种输入输出方向有自动化覆盖。
- 变量编辑、预览、版本恢复、重算版本记录、重名与并发冲突检查已实现。
- 17 项 JS 测试通过；Core 测试／race／vet 通过（发布前再次执行）。
- 360px 浏览器模拟完成变量创建、修改、恢复、原位输出、历史查看、撤销及重载后撤销。
- 真实 WPS 的插件加载和操作仍未验证，按用户新要求交接验收，详见 MACOS_VALIDATION.md。

## 最终构建与安装检查

- Apple Silicon / Intel 安装器均已生成，Mach-O 架构确认分别为 arm64 / x86_64。
- 已用最终 Apple Silicon 安装器更新本机；Core 健康检查通过。
- 已安装的 `/addins/et/`、`/addins/wpp/`、`/addins/wps/` 返回 200，内容与源代码一致；共享侧栏 HTML/JS 同样一致。
- 修复目录入口未返回 index.html 的问题，并增加三个宿主目录入口回归测试。
- 最终 Core 测试与 race 检查通过，Go vet 通过，Core 与安装器 Windows 交叉编译通过，JS 17 项通过，git diff --check 通过。
- 安装器源码 payload 与当前入口及共享侧栏逐文件一致。
- 浏览器验证空间和两个隔离测试服务已关闭；正式 17891 服务保留供用户验收。
- 真实 WPS 测试由用户接手，不能从上述自动化证据推断真实宿主的全部 API 已通过。
