Build-time payload placeholder.

The release pipeline assembles payload/app from the current repository sources and embeds payload/payload.zip. This file only keeps the Go embed root present for installer unit tests.
