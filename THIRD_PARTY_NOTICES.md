# Third-party notices

The runtime bundles Node.js 22.19.0, `@earendil-works/pi-coding-agent` 0.86.0, `quickjs-emscripten` 0.31.0, Acorn 8.15.0 and their production dependencies. Exact package versions are pinned in `agent-host/package-lock.json`; the downloaded Node archive is SHA-256 verified by the packaging script.

The full Node.js license ships as `runtime/LICENSE`. Production dependency license, notice and package metadata files are retained in the installed `agent-host/node_modules` tree.
