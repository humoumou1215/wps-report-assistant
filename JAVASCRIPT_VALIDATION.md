# 0.8.0-js1 验证说明

本版新生成的变量计算与展示逻辑使用 JavaScript，取消新流程中的 Transform/Renderer DSL 能力组合和动态语言回退。旧变量和输出规则仍可执行；在“检查与修改／检查与调整”中重新生成并保存后，会改用新脚本。

## 安装与确认

退出 WPS，运行包内 `DataReportAssistant-Setup-0.8.0-js1.exe` 覆盖安装，无需卸载，项目数据保留。重开 WPS，侧栏应显示 `Core 0.8.0-js1 已连接`。保留 hotfix1 的安装修复：按完整路径停止旧 Core 并等待退出后替换程序。

在设置中配置自己的 AI 服务并启用 AI。无需启用“临时能力”。未配置 AI 时仅保留原有内置回退逻辑，不会生成 JavaScript。

## 优先验证此次问题

1. 打开原来的测试表格，选择包含表头的完整数据范围。
2. 创建变量或检查与修改现有变量，输入：
   `只保留状态为正式的数据，按预算金额从大到小取前五行。第一列为从1开始的序号，后面保留原字段顺序。`
3. 检查实际结果；展开“查看执行脚本”，应显示 JavaScript，不再是能力图或 ra-cap-v1。
4. 对照源文件核对排序。对于本次诊断包的输入，前五个预算金额是：3332600000、12800000、8600000、6200000、5100000。客服部记录真实存在于完整输入，不能因为不在前8行就被判为编造。
5. 若 AI 复核提出疑问，会显示黄色建议；它不再阻止保存。请仍检查实际结果，程序执行成功不代表自动证明业务要求全部正确。
6. 保存变量，修改源文件某个金额并“从源文件重算”：执行保存的脚本，不重复请求 AI 生成。
7. 创建输出，输入展示要求，如金额以万元显示、保留两位小数；生成后先看方案，再应用到原稿。
8. 查看修改历史并撤销，确认内容和格式恢复。变量的旧版本恢复也应可用。

## 执行边界

- AI 编写普通同步 JavaScript 函数体，变量计算使用 rows / columns，展示使用 variable / target。
- 执行引擎随 Core 一起打包，不依赖 Node.js、浏览器 eval 或用户安装运行时。
- 脚本在独立子进程执行，完整输入通过 JSON 传入；不暴露 WPS Application、网络、文件、进程、模块加载或 API Key。
- 代码最大64KB、输入最大5MB、结果最大2MB。JS 内部有1.5秒执行中断，父进程5秒期限；子进程设128MB软内存预算（不是操作系统级硬上限）。
- 原稿写入仍通过宿主适配器、修改前后快照和历史执行。此版本并未开放任意 WPS API 直接修改任意对象；支持范围仍是现有文本和表格对象。
- AI 生成的业务代码仍可能不符合意图；运行错误自动反馈给 AI 修复，最多3次；语义复核仅供用户核对，不再根据不完整样本反复阻断。

## 已验证与未验证

已完成：实际诊断包14行输入重放、前五排序和字段顺序、脚本错误修复、超时和无宿主API访问、变量保存重载重算、展示单位换算、完整/部分证据标识、浏览器侧栏写入和撤销。

浏览器验收使用真实 Core、模拟 WPS 和本地模拟模型响应，不代表已在用户配置的模型和真实 Windows WPS 中验收。请提供实际测试后的诊断包。
