# 文件修改历史与撤销

插件直接修改当前文件，不创建预览副本。统一侧栏的“检查与调整”可修改展示要求并生成方案；点击“应用到原稿”后在 WPS 中查看真实效果。文字、表格和演示共用历史管理器。宿主范围见 [能力扩展说明](HOST_CAPABILITIES.md)，macOS 用户操作见 [验收清单](MACOS_VALIDATION.md)。

“更新此对象”“更新全部”和新建/调整绑定均经过同一修改管理器。顶部快捷操作与“修改历史”页面提供撤销入口。历史详情显示时间、目标、修改要求、修改前后内容及规则/格式记录；批量修改合并为一条历史，逐对象展示结果。

## 持久化与一致性

- `state.json` 的 `pptChanges` 独立于调试日志保存修改前状态、渲染计划和绑定版本，关闭调试不会关闭历史。记录包含业务内容，保留在已有本地数据目录中。
- 写原稿前先持久化 `prepared`；写入成功后读取实际结果，在同一次原子存储更新中提交绑定及 `applied` 状态。客户端重复提交同一操作编号和结果不会重复写入宿主。
- 更新绑定的草稿记录原绑定及变量版本；应用前拒绝过期方案。AI 生成期间及执行准备期间也检查目标是否变化。
- 按当前文稿的操作顺序倒序撤销，批量内部也按倒序恢复。撤销同步恢复之前的绑定规则；撤销新建绑定会移除该绑定。
- 撤销前比较当前内容、已记录文字格式和布局与修改后状态。发现后续修改时停止，不静默覆盖。如果原稿已通过 WPS 自身撤销或放弃保存回到准确的修改前状态，可以只完成历史/绑定恢复。
- 文稿写入和 Core 存储不是跨进程原子事务。`prepared` / `undoing` 中断记录保留并阻止新修改；历史提供显式“检查并恢复”入口。在恢复前提示可能覆盖中断后的手工编辑，保留记录用于检查。
- 有可撤销修改的绑定禁止直接删除，避免破坏恢复链。撤销需要的原变量若已删除，会明确报错。
- 插件不自动保存文件，用户继续使用 WPS 保存。历史持久化不等同于文稿已保存。

## 演示对象的支持范围

- 普通文本框的文本替换；记录字体分段（含中西文字体、大小、粗斜体、颜色等可读取属性）、段落格式及文本框布局。
- 行列数保持不变的表格内容更新；输出要求的相邻单元格合并会一并记录，撤销时先取消合并再恢复原值和格式。
- 写入前阻止：增删表格行、向已有合并区域写入、图片项目符号、文字超链接/交互动作，以及宿主无法提供所需格式接口的对象。不会降级成只恢复纯文字。
- 单对象最多 30,000 字符，单表最多 2,000 个单元格，单批最多 200 个对象。超出限制不写入原稿。
- 原生高级文字效果、特殊模板格式及接口差异需要 Windows WPS 实测后扩展。当前没有承诺任意复杂对象的完整恢复。
- 手工在 WPS 中进行的操作不会写入此历史；插件会对涉及的目标状态进行冲突检查。无重做功能，撤销后可重新生成或更新。

## 自动验证

```sh
cd core-go
go test -race ./...
go vet ./...
cd ..
node --test tests/*.test.cjs
node --check addins/wpp/taskpane.js
node --check addins/wpp/change-state.js
node --check addins/wpp/change-history.js
```

Go 测试覆盖持久化、绑定版本恢复、倒序撤销、文稿隔离、批量部分失败、失败事务回滚、草稿确认与幂等重试。JS 使用模拟宿主检查富文本恢复、表格恢复、能力门禁、手工编辑冲突、网络故障、文稿切换及中断恢复；模拟测试不能替代 WPS 验证。

## Windows WPS 验证清单

1. 新建普通文本绑定，应用、查看历史、撤销，检查原文字/格式恢复且新绑定移除。
2. 混合字体、颜色、多段落及普通项目符号对象，检查恢复前后字体和段落；任何宿主属性读写失败必须显示原因。
3. 固定行列表格应用/撤销，检查行高、单元格格式与内容。使用需要增删行的方案和合并单元格时确认写入前停止。
4. 调整已有绑定要求，应用后撤销；下一次更新应使用之前的规则。
5. 连续应用两次，倒序撤销两次；尝试手工改字、改字体、移动目标后撤销，确认冲突提示且保留手工修改。
6. 批量更新并模拟某一对象不可写，成功项目及失败原因均应显示；撤销恢复成功项目。
7. 修改前断开 Core，确认原稿不变；写入后断开/重启，确认历史可以处理不确定状态，不重复写原稿。
8. 关闭重开插件，确认历史还在；中断恢复需显式确认。关闭文稿但不保存后重开，验证原状识别。
9. 生成、准备、批量执行期间切换文稿，确认不会写入另一文稿。
10. 在 320、350、380px 侧栏中查看、展开历史和长内容，检查页面无整体横向滚动，关键操作可见。
